import { SettingsScreen } from '@/components/SettingsScreen';

export default function AdminSettingsScreen() {
  return <SettingsScreen roleLabel="Administrador" subtitle="Gerencie seu acesso e acompanhe as preferências da operação." />;
}
