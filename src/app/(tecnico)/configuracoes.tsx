import { SettingsScreen } from '@/components/SettingsScreen';

export default function TechnicianSettingsScreen() {
  return <SettingsScreen roleLabel="Técnico" subtitle="Ajuste seu acesso e as preferências para os atendimentos em campo." />;
}
