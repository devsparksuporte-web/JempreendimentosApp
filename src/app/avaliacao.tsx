import { CheckCircle2, ChevronLeft, Circle, CircleX, Send, Star, ThumbsUp, XCircle } from 'lucide-react-native';
import { useState } from 'react';
import { Alert, Pressable, ScrollView, StyleSheet, TextInput, View } from 'react-native';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { useSafeAreaInsets } from 'react-native-safe-area-context';

import { Text } from '@/components/ui/Text';
import { Button } from '@/components/ui/Button';
import { submitServiceRating } from '@/services/ratings';
import { colors, elevation, fonts, layout, radius, spacing, touch } from '@/theme/tokens';

export default function ServiceRatingScreen() {
  const router = useRouter(); const insets = useSafeAreaInsets(); const { id } = useLocalSearchParams<{ id?: string }>();
  const [rating, setRating] = useState(0); const [punctual, setPunctual] = useState<boolean | null>(null); const [condition, setCondition] = useState<'perfeito' | 'parcial' | 'problemas'>('perfeito'); const [feeling, setFeeling] = useState<'triste' | 'neutro' | 'feliz' | 'otimo' | 'apaixonado'>('otimo'); const [comment, setComment] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState<string | null>(null);
  async function submit() { if (!id) { setError('Chamado não informado.'); return; } if (rating === 0) { setError('Escolha uma nota de 1 a 5 estrelas.'); return; } setLoading(true); setError(null); try { await submitServiceRating({ serviceCallId: id, rating, punctual, equipmentCondition: condition, feeling, comment }); Alert.alert('Avaliação enviada', 'Obrigado por avaliar o serviço da JEmpreendimentos.', [{ text: 'Concluir', onPress: () => router.back() }]); } catch (err) { setError(err instanceof Error ? err.message : 'Não foi possível enviar a avaliação.'); } finally { setLoading(false); } }
  return <View style={styles.root}><View style={[styles.header, { paddingTop: insets.top + spacing.lg }]}><Pressable onPress={() => router.back()} style={styles.back}><ChevronLeft size={21} color={colors.textPrimary} /></Pressable><View><Text variant="screenTitle" color={colors.brandStrong}>Avaliar Serviço</Text><Text variant="microLabel" color={colors.brand}>OS {id ? `#${id.slice(0, 8).toUpperCase()}` : 'não informado'}</Text></View></View><ScrollView contentContainerStyle={[styles.content, { paddingBottom: insets.bottom + spacing.xxl }]}><View style={styles.serviceCard}><View style={styles.serviceIcon}><ThumbsUp size={24} color={colors.brand} /></View><View style={styles.serviceText}><Text variant="cardTitle">Serviço concluído</Text><Text variant="body" color={colors.textSecondary}>Sua opinião ajuda a melhorar o atendimento.</Text></View><CheckCircle2 size={22} color={colors.success} /></View><Question label="Como foi seu atendimento?"><View style={styles.stars}>{[1, 2, 3, 4, 5].map((value) => <Pressable key={value} onPress={() => setRating(value)} hitSlop={8}><Star size={34} color={value <= rating ? colors.warning : colors.slate200} fill={value <= rating ? colors.warning : 'transparent'} /></Pressable>)}</View></Question><Question label="O técnico foi pontual?"><View style={styles.twoOptions}><Choice label="SIM" selected={punctual === true} color={colors.success} icon={CheckCircle2} onPress={() => setPunctual(true)} /><Choice label="NÃO" selected={punctual === false} color={colors.dangerStrong} icon={XCircle} onPress={() => setPunctual(false)} /></View></Question><Question label="Equipamento funcionando bem?"><View style={styles.stack}>{([['perfeito', 'Sim, perfeitamente'], ['parcial', 'Parcialmente'], ['problemas', 'Não, apresenta problemas']] as const).map(([value, label]) => <Pressable key={value} onPress={() => setCondition(value)} style={[styles.option, condition === value && styles.optionSelected]}><Text variant="bodyStrong" color={condition === value ? colors.brand : colors.textSecondary}>{label}</Text>{condition === value ? <CheckCircle2 size={20} color={colors.brand} /> : <Circle size={20} color={colors.slate300} />}</Pressable>)}</View></Question><Question label="Qual seu sentimento agora?"><View style={styles.feelings}>{([['triste', '😞'], ['neutro', '😐'], ['feliz', '😊'], ['otimo', '😄'], ['apaixonado', '😍']] as const).map(([value, emoji]) => <Pressable key={value} onPress={() => setFeeling(value)} style={[styles.feeling, feeling === value && styles.feelingSelected]}><Text style={styles.emoji}>{emoji}</Text></Pressable>)}</View></Question><Question label="Deixe seus comentários"><View style={styles.textarea}><TextInput value={comment} onChangeText={setComment} multiline placeholder="Conte-nos mais sobre sua experiência com a JEMPREENDIMENTOS..." placeholderTextColor={colors.slate300} style={styles.input} /></View></Question>{error ? <Text variant="body" color={colors.dangerStrong} style={styles.error}>{error}</Text> : null}<Button label="ENVIAR AVALIAÇÃO" icon={Send} loading={loading} onPress={() => { void submit(); }} /><Pressable onPress={() => router.back()} style={styles.skip}><Text variant="bodyStrong" color={colors.textMuted}>PULAR POR AGORA</Text></Pressable></ScrollView></View>;
}
function Question({ label, children }: { label: string; children: React.ReactNode }) { return <View style={styles.question}><Text variant="microLabel" color={colors.textMuted}>{label}</Text>{children}</View>; }
function Choice({ label, selected, color, icon: Icon, onPress }: { label: string; selected: boolean; color: string; icon: typeof CheckCircle2; onPress: () => void }) { return <Pressable onPress={onPress} style={[styles.choice, selected && { borderColor: color, borderWidth: 2 }]}><Icon size={19} color={selected ? color : colors.textMuted} /><Text variant="bodyStrong" color={selected ? color : colors.textSecondary}>{label}</Text></Pressable>; }
const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: colors.bgApp }, header: { backgroundColor: colors.bgSurface, borderBottomWidth: 1, borderBottomColor: colors.border, paddingHorizontal: layout.screenPadding, paddingBottom: spacing.lg, flexDirection: 'row', alignItems: 'center', gap: spacing.md }, back: { width: 42, height: 42, borderRadius: radius.pill, borderWidth: 1, borderColor: colors.border, alignItems: 'center', justifyContent: 'center' }, content: { width: '100%', maxWidth: layout.maxContentWidth, alignSelf: 'center', padding: layout.screenPadding, gap: spacing.xl }, serviceCard: { backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: 28, padding: spacing.lg, flexDirection: 'row', alignItems: 'center', gap: spacing.md, ...elevation.card }, serviceIcon: { width: 56, height: 56, borderRadius: radius.lg, backgroundColor: colors.brandTint, alignItems: 'center', justifyContent: 'center' }, serviceText: { flex: 1, gap: spacing.xs }, question: { gap: spacing.md }, stars: { backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: 28, padding: spacing.xl, flexDirection: 'row', justifyContent: 'space-between', ...elevation.card }, twoOptions: { flexDirection: 'row', gap: spacing.md }, choice: { flex: 1, minHeight: touch.minTarget, backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: radius.xl, alignItems: 'center', justifyContent: 'center', gap: spacing.sm, flexDirection: 'row' }, stack: { gap: spacing.sm }, option: { minHeight: 54, paddingHorizontal: spacing.lg, backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: radius.xl, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }, optionSelected: { borderColor: colors.brand, borderWidth: 2 }, feelings: { backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: 28, padding: spacing.lg, flexDirection: 'row', justifyContent: 'space-between' }, feeling: { width: 46, height: 46, alignItems: 'center', justifyContent: 'center', borderRadius: radius.pill }, feelingSelected: { backgroundColor: colors.brandTint }, emoji: { fontSize: 27 }, textarea: { minHeight: 130, backgroundColor: colors.bgSurface, borderWidth: 1, borderColor: colors.border, borderRadius: 28, overflow: 'hidden' }, input: { flex: 1, minHeight: 128, padding: spacing.lg, color: colors.textPrimary, fontFamily: fonts.medium, fontSize: 14, textAlignVertical: 'top' }, error: { backgroundColor: colors.dangerSoft, borderRadius: radius.md, padding: spacing.md }, skip: { alignItems: 'center', paddingVertical: spacing.sm } });
