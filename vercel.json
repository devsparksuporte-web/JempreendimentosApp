{
  "buildCommand": "npx expo export -p web && node scripts/pos-build-web.mjs",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/", "destination": "/index.html" },
    { "source": "/(.*)", "destination": "/app.html" }
  ]
}
